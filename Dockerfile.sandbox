FROM python:3.13-slim

# 沙箱运行时依赖：pandas / numpy / duckdb，供 LLM 生成的数据分析代码使用
RUN pip install --no-cache-dir \
    "pandas>=2.1.0" \
    "numpy>=1.24.0" \
    "duckdb>=0.10.0"

# 沙箱仅作为执行环境，无需 CMD，由 SandboxExecutor 通过 docker run 调用
CMD ["python3"]
